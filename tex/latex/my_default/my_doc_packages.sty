%% Reference common packages here.
%% This package should be after document class definition.
%% Arguments can be used here.

%doc%
%doc% The widely used package to use graphical images within a \LaTeX{} document.
%doc%
\usepackage[pdftex]{graphicx}

%doc%
%doc% This package offers strikethrough command foobar.
%doc%
\usepackage[normalem]{ulem}

%doc% Create framed, shaded, or differently highlighted regions that can break across pages.
%doc%
\usepackage{framed}

%doc% This package replaces the built-in definitions for enumerate, itemize and description.
%doc% With \texttt{enumitem} the user has more control over the layout of those environments.
%doc%
\usepackage{eso-pic}

%doc%
%doc% This package is used to generate blind text for demonstration purposes.
%doc%
\usepackage{units}


%doc%
%doc% This package is used for landscape tables.
%doc%
\usepackage{lscape}
\usepackage{makecell}


%doc%
%doc% This package is used for centering vertically in tables.
%doc%
\usepackage{mwe}
\usepackage{graphbox} 


%doc%
%doc% This package is used for keeping figures below sections.
%doc%
\usepackage{placeins}


\usepackage{eurosym}
\usepackage{listings}


%% ========================================================================
%%%% MISC usepackages
%% ========================================================================
\usepackage{glossaries}					%% list of abbreviations
\usepackage{rotating}					%% landscape figures, tables, ...
\usepackage{booktabs}					%% professionally looking tables
\usepackage{array}						%% tables with fix width/left align
\usepackage{subcaption}					%% use figures side by side
\usepackage{fancyhdr}					%% footer line on every page
\usepackage{pdfpages}					%% insert pdfs
\usepackage{forest}						%% generate file tree
